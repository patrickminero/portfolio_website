<div class="w-full h-screen md:h-full flex flex-col md:flex-row items-center justify-center gap-4 px-8 md:p-0">
  <%= image_tag 'patrick_minero.jpg', alt: "patrick minero photo", class: "w-64 h-64 rounded-full object-cover" %>
  <div class="flex flex-col gap-4">
    <h1 class="text-xxl font-bold text-gray-600 text-center md:text-left"><%= @profile.name %></h1>
    <h2 class="text-xl font-semibold text-gray-500 text-center md:text-left"><%= @profile.description %></h2>
  <div>
</div>
