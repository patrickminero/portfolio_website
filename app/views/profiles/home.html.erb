<div class="w-full h-full flex items-center gap-4">
  <%= image_tag 'patrick_minero.jpg', alt: "patrick minero photo", class: "w-64 h-64 rounded-full object-cover" %>
  <div class="flex flex-col gap-4">
    <h1 class="text-xxl font-bold text-gray-600"><%= @profile.name %></h1>
    <h2 class="text-xl font-semibold text-gray-500"><%= @profile.description %></h2>
  <div>
</div>
